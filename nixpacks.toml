[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install --break-system-packages -r requirements.txt"]

[start]
cmd = "PYTHONPATH=/app/src panel serve dashboard.py --address 0.0.0.0 --port ${PORT:-8080} --allow-websocket-origin='*' --num-procs 1"
